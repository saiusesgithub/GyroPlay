# Contributing

Thanks for helping with GyroPlay.

Start with `docs/contributing.md` for the project layout, local checks, and pull request expectations.

Before opening a pull request:

- Keep the change focused.
- Run the checks for the project area you changed.
- Update protocol and setup documentation when behavior changes.
- Do not include generated build outputs, local virtual environments, or secrets.

